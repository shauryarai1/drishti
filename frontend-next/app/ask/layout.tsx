import React from 'react';

/**
 * Public route. Ask KAVACH does not use a birth profile and is usable without
 * an account; signing in is an optional convenience for saving history.
 */
export default function AskLayout({ children }: { children: React.ReactNode }) {
  return children;
}
